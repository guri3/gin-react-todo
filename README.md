# gin-react-todo

* 動作手順

```sh
# src/api内で
$ go run main.go
# client内で
$ npm install # 最初だけ
$ node_modules/.bin/webpack-dev-server --hot
```


* 動作確認URL

  * Gin http://localhost:3030/todos

  * React http://localhost:8080
